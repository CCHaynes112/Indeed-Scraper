Indeed Scraper

The program performs a search by taking in a job title and a location. It scans every listing on x amount of pages, and returns the most frequent words that are used, and how frequently they appear. It's function is to output the most frequent keywords that employers use for a given field.


Prerequisites

You'll need to pip install selenium
